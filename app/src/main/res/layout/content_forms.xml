<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:grid="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    grid:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="vpchc.prohealth.FormsActivity"
    tools:showIn="@layout/activity_forms">
    <android.support.v7.widget.GridLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        grid:columnCount="1"
        grid:rowCount="3"
        grid:orientation="horizontal"
        android:layout_marginTop="90dp"
        android:id="@+id/homeGrid">

    <Spinner
        android:id="@+id/spinnerFormsLocations"
        style="@style/fancySpinnerDropDown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"/>

    <Spinner
        android:id="@+id/spinnerFormsCategories"
        style="@style/fancySpinnerDropDown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"/>

    <Spinner
        android:id="@+id/spinnerFormsSelection"
        style="@style/fancySpinnerDropDown"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    </android.support.v7.widget.GridLayout>
</RelativeLayout>
